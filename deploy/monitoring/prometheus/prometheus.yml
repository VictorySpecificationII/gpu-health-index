global:
  scrape_interval: 5s
  evaluation_interval: 5s

rule_files:
  - alerts.yml

scrape_configs:
  - job_name: gpu-health
    file_sd_configs:
      - files: ['/var/run/gpu-health/file_sd.json']
        refresh_interval: 30s
